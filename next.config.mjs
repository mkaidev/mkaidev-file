/** @type {import('next').NextConfig} */
const nextConfig = {
  images: {
    remotePatterns: [
      {
        hostname: "earnest-crane-961.convex.cloud",
      },
    ],
  },
};

export default nextConfig;
